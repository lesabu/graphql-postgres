{
  "name": "express-graphql",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon index.js",
    "setup": "process.env.NODE_ENV",
    "db:setup": "cd sql && psql -d test -f setup.sql"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.5.0",
    "pg-promise": "^10.9.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
